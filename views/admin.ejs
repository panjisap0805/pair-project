<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
  <title><% title %></title>
</head>
<body>
  <table>
    <!-- Lihat orderan dari user semua -->
    <thead>
      <tr>
        <th>Transaction Id</th>
        <th>Username</th>
        <th>Product Name</th>
        <th>Total Price</th>
      </tr>
    </thead>
    <tbody>
      <% for (let i = 0; i < dataTransaction.length; i++) { %> 
        <tr>
          <td><%= dataTransaction[i].id %></td>
          <td><%= dataTransaction[i].User.username %></td>
          <td><%= dataTransaction[i].Product.productFullname %></td>
          <td><%= dataTransaction[i].Product.price * dataTransaction[i].total %></td>
        </tr>
      <% } %>
    </tbody>
  </table>
</body>
</html>